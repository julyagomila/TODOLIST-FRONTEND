const Header = () => {
  return (
    <header>
      <span className="titleHeader">
        Lorsque l'on a trop de choses à faire en même temps l'on ne fait rien
      </span>
    </header>
  );
};

export default Header;
